# v0.3.0 Dogfood CEO Brief - 2026-05-20

## Goal

Close the AEL-owned findings from the first Qing Ideology v0.3.0 dogfood
without starting a new sprint: land the wrapper hotfixes, document the
installed-wrapper drift, verify the local test surface, and leave AiPlus-owned
substrate issues clearly handed off.

## Prompt for CEO

You are reviewing the first real AEL v0.3.0 dogfood pass from
`~/Research/A1_Qing_Ideology`.

Read:

1. `docs/dev/v0.3.0-dogfood-2026-05-20.md`
2. `docs/dev/v1.0-design.md`
3. Current diff for:
   - `ael`
   - `bin/ael.ps1`
   - `tests/ael_wrapper.test.sh`
   - `tests/ael_ps1.test.ps1`
   - `CHANGELOG.md`

Review objective:

- Decide whether the AEL-local hotfix is correct and sufficiently scoped for
  dogfood continuation.
- Confirm the source wrapper behavior fixes the two AEL-owned issues:
  `ael "我想反思 RD 设计"` delegates to `aiplus agent talk <input>`, and
  direct role shortcuts such as `ael pi --fresh` do not leak `--fresh` into
  aiplus as an unexpected argument.
- Confirm the dogfood document correctly classifies installed-wrapper/PATH
  drift: the Owner used `/Users/steve/.local/bin/ael` reporting `AEL 0.2.10`,
  while this repo source reports `AEL 0.3.0 (aiplus 0.6.19+)`.
- Confirm the remaining issues are correctly assigned to AiPlus substrate:
  brand leak in `✦ aiplus: project is set up.`, `agent_route` record-vs-run
  semantics, cross-role memory bridge evidence, and runtime-default friction.

Constraints:

- Do not start a new sprint.
- Do not delete bash `detect_role_from_input` yet; keep it until dogfood proves
  aiplus `role_aliases` works end to end.
- Do not unpark cross-runtime handoff until there are at least three transcript
  continuity incidents.
- Do not edit `vendor/aiplus` from the AEL side for this pass.
- Do not classify stale installed-wrapper behavior as a fresh 0.3.0 source
  regression.

Evidence already gathered:

- `bash tests/ael_wrapper.test.sh` passes.
- `bash -n ael` passes.
- `bash -n scripts/build-ael.sh` passes.
- `./ael --version` prints `AEL 0.3.0 (aiplus 0.6.19+)`.
- PowerShell tests were updated but not executed on this Mac because neither
  `pwsh` nor `powershell` is installed.

Decision requested:

1. Approve this as a small dogfood hotfix ready to commit, or name the exact
   additional local check required before commit.
2. Confirm whether the next operational step is publishing/installing the
   0.3.0 wrapper so the Owner's next dogfood terminal no longer runs 0.2.10.
3. Keep all AiPlus-owned issues in the separate AiPlus prompt; do not solve
   them inside AEL during this dogfood pass.
